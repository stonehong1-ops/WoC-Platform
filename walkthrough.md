# 라이브 페이지(/live) UI 결함 수정 및 인트로 대기화면 전면 제거 완료 보고서

라이브 포털 홈 화면의 디자인 깨짐(폰트, 여백), 썸네일 이미지 엑박 결함 및 라이브 피드 진입 시의 불필요한 인트로 대기 장벽(DIVE IN 오버레이) 제거 수정을 완료했습니다.

---

## 1. 개선 내역

### 1) 라이브 포털 홈 디자인 깨짐 수정
- **폰트 무결성**: `@theme`에 등록되지 않아 시스템 기본 고딕체로 깨져 보이던 `font-manrope` 클래스를 Stitch 디자인 시스템 표준 폰트인 `font-body` (Inter)로 교체하여 폰트 렌더링을 깔끔하게 바로잡았습니다.
- **레이아웃 여백**: 꼬여서 여백이 0px로 찌그러져 렌더링되던 비표준 `px-page-margin` 및 `-mx-page-margin` 클래스를 Stitch 디자인 규격인 `px-4 md:px-6` 및 `-mx-4 md:-mx-6` 반응형 마진으로 전면 치환하여 화면 양옆 여백을 복원했습니다.

### 2) 비디오 포스트 썸네일 엑박 박멸
- **동적 미디어 분기**: 최근 영상 리스트 및 추천 섹션에서 Firestore 스캔 데이터가 비디오 확장자(`.mp4`, `.mov`, `video` 단어 포함 등)일 경우, `<img>` 대신 `<video>` 태그(자동 무음 playsInline 루프 렌더링)로 분기 렌더링되도록 수정했습니다.
- **결과**: 비디오 파일이 들어왔을 때 엑박이 뜨던 현상을 완벽히 해결하고, 마우스 호버 및 모바일 뷰에서 움직이는 고화질 비디오 프리뷰가 직관적으로 노출되는 고급 UI/UX를 달성했습니다.

### 3) "DIVE IN" 인트로 대기 화면 전면 삭제
- **다이렉트 진입 엔진**: `LiveFeed.tsx`에 진입할 때 무조건 1회 클릭 및 로딩을 거쳐야만 비디오 스트림이 켜지던 `showDashboardIntro` 상태 초기값을 `false`로 고정했습니다.
- **결과**: 라이브 포털 홈에서 입장/재생을 클릭하자마자 검은색 대기 페이지 장벽 없이 즉각적인 실시간 탱고 비디오 스트리밍 화면이 1초 만에 실행 및 재생되도록 전환 처리했습니다.

---

## 2. 검증 완료 내역

## 검증 결과 (TDD)
- **Foreground xxxhdpi (432x432) 가로 로고폭**: 271px (전체 대비 62.73% 비율로 지정 비율인 63% 최적 안착).
- **모서리 투명도 스캔**: 4개 꼭짓점의 알파 채널 값이 `0`으로 확인되어 외곽 테두리선이 남김없이 제거된 투명 캔버스 상태 확인.
- **배경 무결성**: Legacy 아이콘의 배경이 완벽한 흰색 단색(`#FFFFFF`)임을 확인.
- **앱 이름 한글/영어 정합성**:
  - `values-ko/strings.xml`: 한글 이름을 물음표 깨짐 없이 `탱고월드`로 복구 완료.
  - `values/strings.xml`: 영문 기본 이름을 띄어쓰기 없이 `TangoWorld`로 원복 완료.
- **기기 OS 플랫폼 수집 기능 탑재**: 
  - `AuthProvider.tsx` 및 `types/user.ts` 파일에 기기 플랫폼 자동 감지 로직을 수립하여 실서버 릴리즈 완료 (앱 재배포 없이 유저 접속 시 `android` | `ios` | `web` 상태 자동 소급 수집).
- **Gradle 빌드 및 AAB**: `versionCode 11` 및 최종 63% 스케일 로고가 반영된 `app-release.aab` 파일이 최종 **클린 빌드(Clean Build)** 완료됨 (크기: 약 200.23MB).
- **프로젝트 버전**: `2026-07-14-003`.
- **Next.js 빌드 (`npm run build`)**: **[성공]** (다국어 및 컴파일 린트 에러 없음)
- **Capacitor 동기화 (`npx cap sync android`)**: **[성공]** (안드로이드 자산 동기화 완료)
- **AAB 빌드 테스트 무결성**: 네이티브 컴파일 구성 및 서명 프로퍼티 연동이 완벽하게 마무리되었습니다.
